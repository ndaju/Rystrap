<ui:UiPage x:Class="Rystrap.UI.Elements.Settings.Pages.BehaviourPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Rystrap.UI.Elements.Settings.Pages"
      xmlns:controls="clr-namespace:Rystrap.UI.Elements.Controls"
      xmlns:models="clr-namespace:Rystrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:Rystrap.Resources"
      d:DataContext="{d:DesignInstance Type=models:BehaviourViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="BehaviourPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock Margin="0,0,0,8" Text="{x:Static resources:Strings.Menu_Behaviour_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_ConfirmLaunches_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_ConfirmLaunches_Description}">
            <ui:ToggleSwitch IsChecked="{Binding ConfirmLaunches, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_BackgroundUpdates_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_BackgroundUpdates_Description}">
            <ui:ToggleSwitch IsChecked="{Binding BackgroundUpdates, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Description}">
            <controls:OptionControl.Style>
                <Style TargetType="controls:OptionControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRobloxInstallationMissing, Mode=OneTime}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:OptionControl.Style>
            <ui:ToggleSwitch IsChecked="{Binding ForceRobloxReinstallation, Mode=TwoWay}" />
        </controls:OptionControl>

        <TextBlock Text="Bootstrapper Sounds" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <controls:OptionControl Header="Start Sound" Description="Play a sound when the bootstrapper starts.">
            <StackPanel Orientation="Horizontal">
                <ui:Button Icon="DocumentAdd16" Content="Browse..." Command="{Binding BrowseStartSoundCommand}" Margin="0,0,6,0" />
                <ui:Button Icon="Delete16" Content="Clear" Appearance="Danger" Command="{Binding ClearStartSoundCommand}" Visibility="{Binding ShowClearStartSound, Mode=OneWay}" />
                <TextBlock Text="{Binding BootstrapperStartSoundDisplay, Mode=OneWay}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl Header="Error Sound" Description="Play a sound when the bootstrapper encounters an error.">
            <StackPanel Orientation="Horizontal">
                <ui:Button Icon="DocumentAdd16" Content="Browse..." Command="{Binding BrowseErrorSoundCommand}" Margin="0,0,6,0" />
                <ui:Button Icon="Delete16" Content="Clear" Appearance="Danger" Command="{Binding ClearErrorSoundCommand}" Visibility="{Binding ShowClearErrorSound, Mode=OneWay}" />
                <TextBlock Text="{Binding BootstrapperErrorSoundDisplay, Mode=OneWay}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
            </StackPanel>
        </controls:OptionControl>

        <controls:OptionControl Header="Complete Sound" Description="Play a sound when the bootstrapper finishes.">
            <StackPanel Orientation="Horizontal">
                <ui:Button Icon="DocumentAdd16" Content="Browse..." Command="{Binding BrowseCompleteSoundCommand}" Margin="0,0,6,0" />
                <ui:Button Icon="Delete16" Content="Clear" Appearance="Danger" Command="{Binding ClearCompleteSoundCommand}" Visibility="{Binding ShowClearCompleteSound, Mode=OneWay}" />
                <TextBlock Text="{Binding BootstrapperCompleteSoundDisplay, Mode=OneWay}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
            </StackPanel>
        </controls:OptionControl>

        <TextBlock Text="Appearance" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />

        <controls:OptionControl Header="Progress Bar Color" Description="Customize the bootstrapper progress bar color (hex, e.g. #4CAF50). Defaults to green.">
            <TextBox Text="{Binding BootstrapperProgressColor, Mode=TwoWay}" Width="140" Padding="8,4" />
        </controls:OptionControl>

        <controls:OptionControl Header="Background Image" Description="Set a background image for the bootstrapper window.">
            <StackPanel Orientation="Horizontal">
                <ui:Button Icon="DocumentAdd16" Content="Browse..." Command="{Binding BrowseBootstrapperBackgroundImageCommand}" Margin="0,0,6,0" />
                <ui:Button Icon="Delete16" Content="Clear" Appearance="Danger" Command="{Binding ClearBootstrapperBackgroundImageCommand}" Visibility="{Binding ShowClearBootstrapperBackgroundImage, Mode=OneWay}" />
                <TextBlock Text="{Binding BootstrapperBackgroundImageDisplay, Mode=OneWay}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" />
            </StackPanel>
        </controls:OptionControl>
    </StackPanel>
</ui:UiPage>
